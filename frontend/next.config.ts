import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com','localhost', 'images.unsplash.com', 'img.icons8.com', 'source.unsplash.com'],
  },
};

export default nextConfig;
